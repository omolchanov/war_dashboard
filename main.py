"""
WarDashboard starter script.

Right now this just prints a message and serves as a
single entry point. We can later expand it into a real
app (API, UI, data pipeline, etc.).
"""

APP_NAME = "WarDashboard"
APP_VERSION = "0.1.0"


def main() -> None:
    print(f"{APP_NAME} v{APP_VERSION} starting up...")
    
    # TODO: initialize backend API, data pipeline, and UI here
    print("Nothing wired up yet — this is just a starter script.")
    print("Good place to begin implementing your architecture plan.")


if __name__ == "__main__":
    main()

    